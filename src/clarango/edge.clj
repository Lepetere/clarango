(ns clarango.edge
  (:require [clarango.core :as clarango.core]
            [clarango.utilities.core-utility :as core-utility]
            [clarango.utilities.graph-utility :as graph-utility]
            [clarango.utilities.http-utility :as http]))

(defn get-by-key
  "Gets an edge by it's key."
  [& args]
  nil)

(defn replace-by-key
  "Replace an edge by it's key."
  [& args]
  nil)

(defn update-by-key
  "Updates an edge by it's key."
  [& args]
  nil)

(defn delete-by-key
  "Deletes an edge by it's key."
  [& args]
  nil)