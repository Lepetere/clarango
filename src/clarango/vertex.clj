(ns clarango.vertex
  (:require [clarango.core :as clarango.core]
            [clarango.utilities.core-utility :as core-utility]
            [clarango.utilities.graph-utility :as graph-utility]
            [clarango.utilities.http-utility :as http]))

(defn get-by-key
  "Gets a vertex by it's key."
  [& args]
  nil)

(defn replace-by-key
  "Replace a vertex by it's key."
  [& args]
  nil)

(defn update-by-key
  "Updates a vertex by it's key."
  [& args]
  nil)

(defn delete-by-key
  "Deletes a vertex by it's key."
  [& args]
  nil)